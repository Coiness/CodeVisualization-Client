import { Snapshot } from "../../view/project";

export const defaultSnapshot = {
  widgetManagerModel: {
    id: "widgetRenderer",
    width: 700,
    height: 700,
    widgets: [],
    color: "#666",
  },
} as Snapshot;
